{{$smarty.template}|helper:'page':'registerTemplate':'wd'}
{'Browsers'|helper:'page':'setBreadcrumb'}
{'DOM - Document Object Model'|helper:'page':'headline'}
<div class="row">
    <div class="col-lg-12 col-md-12">
        {$page->headline}
        <p>The <strong>Document Object Model</strong> (<strong>DOM</strong>) is a cross-platform and language-independent convention for representing and interacting with objects in {'HTML'|link_urls:'w'}, {'XHTML'|link_urls:'w'} and {'XML'|link_urls:'w'} documents.</p>
        <p>Objects in the DOM tree may be addressed and manipulated by using methods on the objects.</p>
        <p>The public interface of a DOM is specified in its application programming interface ({'API'|link_urls:'i'}).</p>
        <p>The history of the Document Object Model is intertwined with the history of the "{'browser wars'|link_urls:'w'}" of the late 1990s between {'Netscape Navigator'|link_urls:'w'} and Microsoft {'Internet Explorer'|link_urls:'w'}, as well as with that of {'JavaScript'|link_urls:'/l'} and {'JScript'|link_urls:'/l'}, the first scripting languages to be widely implemented in the layout engines of web browsers.</p>
    </div>
</div>